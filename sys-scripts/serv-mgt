pstree : process tree, startign from systemd

sudo systemctl --type=service : List active system services
sudo systemctl status sshd : Look at one service in detail (--user)
sudo systemctl list-dependencies network.service : lit service deps

sudo systemctl stop sshd : Stop a service (stop/start/restart)
sudo systemctl enable nginx : start the service after reboot (--user)
sudo systemctl disable nginx : stop the service on reboot (--user)

sudo journalctl -u nginx -f : real-time logs of a service, resolve service conflicts

START -- STOP -- RESTART -- STATUS -- ENABLE -- DISABLE 

----WRITE A CUSTOM SERVICE----
------------------------------

sudo nano /usr/local/bin/<script>.sh : create a script
chmod +x <script>.sh : give execute permission 
sudo nano /etc/systemd/system/<script>.service : create a service file

--EXAMPLE--
[Unit]
Description=FOSSLinux Service

[Service]
ExecStart=/usr/local/bin/Hello_FOSSLinux.sh

[Install]
WantedBy=multi-user.target
-----------

sudo systemctl daemon-reload : reload the systemd daemon 
systemctl start <script>.service : start the service
systemctl enable <script>.service : enable the service 
systemctl status <script>.service : status the service 

sudo journalctl -u <script>.service -f : see the logs of the service 

blogs:
https://www.fosslinux.com/111815/a-guide-to-creating-linux-services-with-systemd.htm
https://linuxhandbook.com/create-systemd-services/
