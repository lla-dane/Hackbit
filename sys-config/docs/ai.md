-
-
-

- vector DB, represents data in multidimensional space, ideal for applications requiring rapid and accurate matching based on similarity rather than exact values

- vectoristaion: words, images or audio -> numerical vector embeddings, where each data point high-dimensional space

- vector: ordered list of numbers, embeddings are generated by ML models to represent compelx data like text, images etc4